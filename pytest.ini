[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    e2e: End-to-end HTTP tests (slower, runs in CI)
    unit: Unit tests (fast)
    integration: Integration tests (medium speed)

